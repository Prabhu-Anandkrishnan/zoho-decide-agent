  <div data-zs-pricing-container class="price-mask">
    {% if product.has_variants && !product.variants[0].hide_price %}
      <div class="theme-product-detail-price-area" data-zs-pricings data-zs-variant-id="-1">
        <div class="theme-prices-inner-container">
          {% if product.has_variant_price %}
            <span class="theme-product-price">
                <div data-zs-selling-price="{{ product.starts_with }}" data-zs-currency-code="{{currency.code}}"> {{ product.starts_with | currency(currency) }} </div>
            </span>
          {% elif product.label_price && (product.label_price >= product.selling_price) %}
            <div class="theme-product-price" data-zs-selling-price="{{ product.selling_price }}" data-zs-currency-code="{{currency.code}}">
                {{ product.selling_price | currency(currency) }}
            </div>
            <div class="theme-product-old-price" data-zs-label-price="{{ product.label_price }}" data-zs-currency-code="{{currency.code}}" {% if product.label_price == product.selling_price %}style="display: none;"{% endif %}>
                {{ product.label_price | currency(currency) }}
            </div>
          {% else %}
            <div class="theme-product-price" data-zs-selling-price="{{ product.selling_price }}" data-zs-currency-code="{{currency.code}}">
                {{ product.selling_price | currency(currency) }}
            </div>
          {% endif %}
        </div>
      </div>
      {% for variant in product.variants %}
        {% if !variant.hide_price %}
        <div class="theme-product-detail-price-area" data-zs-pricings data-zs-variant-id = "{{ variant.variant_id }}" style="display: none">
          <div class="theme-prices-inner-container">
            <div class="theme-product-price" data-zs-selling-price="{{ variant.selling_price }}" data-zs-currency-code="{{currency.code}}">
              {{ variant.selling_price | currency(currency) }}
            </div>
            {% if variant.label_price && (variant.label_price >= variant.selling_price) %}
              <div class="theme-product-old-price" data-zs-label-price="{{ variant.label_price }}" data-zs-currency-code="{{currency.code}}" {% if variant.label_price == variant.selling_price %}style="display: none;"{% endif %}>
                {{ variant.label_price | currency(currency) }}
              </div>
            {% endif %}
          </div>
        </div>
        {% endif %}
      {% endfor %}
    {% else %}
      {% assign variant = product.variants[0] %}
      <div class="theme-product-detail-price-area" data-zs-pricings data-zs-variant-id = "{{ variant.variant_id }}">
        <div class="theme-prices-inner-container">
          {% if variant.label_price && (variant.label_price >= variant.selling_price)  && !variant.hide_price %}
            <div class="theme-product-price" data-zs-selling-price="{{ variant.selling_price }}" data-zs-currency-code="{{currency.code}}">
              {{ variant.selling_price | currency(currency) }}
            </div>
            <div class="theme-product-old-price" data-zs-label-price="{{ variant.label_price }}" data-zs-currency-code="{{currency.code}}" {% if variant.label_price == variant.selling_price %}style="display: none;"{% endif %}>
              {{ variant.label_price | currency(currency) }}
            </div>
          {% else %}
            {% if !variant.hide_price %}
            <div class="theme-product-price" data-zs-selling-price="{{ variant.selling_price }}" data-zs-currency-code="{{currency.code}}">
              {{ variant.selling_price | currency(currency) }}
            </div>
            {% endif %}
          {% endif %}
        </div>
      </div>
    {% endif %}
  </div>

  <div data-zs-pricelist class="theme-prod-pricelist-outter">
    {% include 'store-product-pricelist' %}
  </div>
